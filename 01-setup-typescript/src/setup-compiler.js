// make tsc globally available ```npm install typescript -g```
// create default tsconfig.json ```tsc --init``` 
// open tsconfig.json
// add files under ```src``` to typescript
// The next line should add Hello from typescript to the body
document.getElementById('setup-compiler').innerHTML += 'Hello from typescript';
